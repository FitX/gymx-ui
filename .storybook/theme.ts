import { create } from '@storybook/theming/create';
// import Logo from './betterthannothing.png';

export default create({
  base: 'dark',
  brandTitle: 'GymX UI',
  colorPrimary: 'var(--color-accent)',
  // brandUrl: 'https://example.com',
  // brandImage: Logo,
});
